import { style } from '@vanilla-extract/css';

export const disclosureCls = style({
  display: 'block'
});

export const toggleCls = style({
  display: 'block',
  padding: '1rem'
});

export const contentCls = style({
  padding: '1rem'
});
